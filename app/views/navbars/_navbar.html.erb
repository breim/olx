<!-- Fixed navbar -->
<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Olx</a>
    </div>
    <div id="navbar" class="navbar-collapse collapse">

      <div class="col-sm-3 col-md-3">
        <%= form_tag search_products_path, class: 'navbar-form', method: :get do %>
          <div class="input-group">
            <%= text_field_tag :search, params[:search], class: "form-control", :placeholder => "Busca" %>
            <div class="input-group-btn">
              <%= button_tag(type: "submit", class: "btn btn-default", name: nil) do %>
                  <i class="fa fa-search"></i>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>

      <ul class="nav navbar-nav">  </ul>
      <ul class="nav navbar-nav navbar-right">

        <%= link_to "#{icon('plus')} Inserir anúncio".html_safe, new_product_path, class: 'btn btn-warning navbar-btn' %>

        <% if current_user %>
        <li class="dropdown">
         <a href="#" class="dropdown-toggle" data-toggle="dropdown">
          <%= current_user.email %>
        </a>
        <ul class="dropdown-menu" role="menu">
          <li><%= link_to "Mensagens", comments_path %></li>
          <li><%= link_to "Meus produtos", myproducts_products_path %></li>
          <li><%= link_to "Administração", admin_path if current_user.admin %></li>
          <li role="separator" class="divider"></li>
          <li><%= link_to "Sair", destroy_user_session_path, :method => :delete %></li>
        </ul>
      </li>
      <% else %>
      <%= link_to "Entrar", new_user_session_path , class: "btn btn-primary navbar-btn'" %>
      <% end %>
    </ul>
  </div><!--/.nav-collapse -->
</div>
</nav>
