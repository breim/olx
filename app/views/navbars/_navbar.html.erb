<!-- Fixed navbar -->
<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Olx</a>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">  </ul>
      <ul class="nav navbar-nav navbar-right">
      <% if current_user %>
        <li class="dropdown">
         <a href="#" class="dropdown-toggle" data-toggle="dropdown">
          <%= current_user.email %>
        </a>
        <ul class="dropdown-menu" role="menu">
          <li><%# link_to "Backoffice",admin_path if current_user.admin %></li>
          <li role="separator" class="divider"></li>
          <li><%= link_to "Sair", destroy_user_session_path, :method => :delete %></li>
        </ul>
      </li>
      <% else %>
      <li>
        <div class="navbar-btn">
          <%= link_to "#{icon('plus')} Inserir anúncio".html_safe, new_product_path, class: 'btn btn-warning' %>
          <%= link_to "Entrar", new_user_session_path , class: "btn btn-primary" %>
        </div>
      </li>
      <% end %>
    </ul>
  </div><!--/.nav-collapse -->
</div>
</nav>
