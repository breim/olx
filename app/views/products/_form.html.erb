<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price %>
    <%# f.text_field :value, prepend: "R$" %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>

  <%= f.file_field :image %> 
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<script>
  /*VMasker(document.querySelector("#product_price")).maskMoney({
    precision: 2, 
    separator: ',', 
    delimiter: '.', 
    zeroCents: false
  });*/
</script>
